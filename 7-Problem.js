// 7-Problem statement - Create three variables of types string, number, and boolean, and log their values.

const fullName = "Anirudha Bele";

const age = 23;

const isMarried = false;

console.log(`Type of "string" variable: ${typeof fullName}`);

console.log(`Type of "number" variable: ${typeof age}`);

console.log(`Type of "boolean" variable: ${typeof isMarried}`);
